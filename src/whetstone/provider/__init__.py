"""Model access, behind one interface.

Nothing outside this package knows which provider is in use or what flags it
takes. See `base.py` for the contract and `claude_cli.py` for the first
implementation.
"""
