"""The rendered-UI lens: defects that only exist when the app is running.

Everything browser-shaped in this project lives under this package, and
`tests/unit/test_spine_is_lens_agnostic.py` is what keeps it that way -- written
before this package existed, so it measures rather than describes.
"""
